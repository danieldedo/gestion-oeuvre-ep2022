<form class="" method="post" action="{{ path('app_oeuvre_delete', {'id': oeuvre.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ oeuvre.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
